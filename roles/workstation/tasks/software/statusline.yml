- name: include Waybar tasks for Wayland displays
  tags: statusline,waybar
  import_tasks: statusline-waybar.yml
  when: status_bar == "waybar"

- name: include Polybar tasks for X11 displays
  tags: statusline,polybar
  import_tasks: statusline-polybar.yml
  when: status_bar == "polybar"
